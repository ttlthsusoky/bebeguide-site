# 🤖 베베가이드 AI 챗봇 설정 가이드

## ✅ 완료된 작업

1. ✅ **챗봇 UI 추가** - 오른쪽 하단 떠있는 버튼
2. ✅ **OpenAI API 연동** - GPT-4o-mini 모델 사용
3. ✅ **참고사이트 정보 포함** - KDCA, WHO, 대한소아과학회 등 8곳
4. ✅ **응급 상황 감지** - 고열, 호흡곤란 등 즉시 119 안내
5. ✅ **로딩 애니메이션** - 답변 생성 중 표시

---

## 🚀 배포 방법 (3단계)

### 1단계: OpenAI API 키 발급

1. **OpenAI 계정 생성**
   - https://platform.openai.com 접속
   - 회원가입 (이메일 인증 필요)

2. **API 키 발급**
   - Dashboard → API Keys → "Create new secret key"
   - 키 복사 (한 번만 표시됨!) 예: `sk-proj-xxxxx...`

3. **결제 정보 등록** (필수!)
   - Billing → Add payment method
   - 신용카드 등록 (최소 $5 충전 권장)
   - **예상 비용**: 대화 100건당 약 $0.10 (100원 정도)

---

### 2단계: Cloudflare Worker 배포

#### 방법 A: Cloudflare Dashboard 사용 (추천, 간편함!)

1. **Cloudflare 로그인**
   - https://dash.cloudflare.com 접속

2. **Worker 생성**
   - 왼쪽 메뉴: `Workers & Pages` → `Create application` → `Create Worker`
   - 이름: `bebeguide-chatbot` (원하는 이름)
   - `Deploy` 클릭

3. **코드 교체**
   - `Edit code` 버튼 클릭
   - 기존 코드 전체 삭제
   - `C:\Users\hee\website\cloudflare-worker.js` 파일 내용 전체 복사
   - 붙여넣기 후 `Save and Deploy` 클릭

4. **환경 변수 설정**
   - 상단 탭: `Settings` → `Variables` → `Environment Variables`
   - `Add variable` 클릭:
     - Variable name: `OPENAI_API_KEY`
     - Value: `sk-proj-xxxxx...` (1단계에서 복사한 키)
     - `Encrypt` 체크 (중요!)
   - `Save and deploy` 클릭

5. **Worker URL 확인**
   - 예시: `https://bebeguide-chatbot.your-account.workers.dev`
   - **이 URL을 복사하세요!**

---

#### 방법 B: Wrangler CLI 사용 (개발자용)

```bash
# 1. Wrangler 설치
npm install -g wrangler

# 2. Cloudflare 로그인
wrangler login

# 3. wrangler.toml 파일 생성 (프로젝트 루트)
cat > wrangler.toml << 'EOF'
name = "bebeguide-chatbot"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

[vars]
# 환경 변수는 Dashboard에서 설정
EOF

# 4. 배포
wrangler deploy

# 5. 환경 변수 설정
wrangler secret put OPENAI_API_KEY
# 프롬프트에 API 키 입력: sk-proj-xxxxx...
```

---

### 3단계: 웹사이트 코드 수정

1. **Worker URL 확인**
   - Cloudflare Dashboard에서 복사한 URL
   - 예: `https://bebeguide-chatbot.your-account.workers.dev`

2. **script.js 파일 수정**
   - 파일 경로: `C:\Users\hee\website\js\script.js`
   - 1154줄 찾기:

```javascript
// 현재 (가상의 URL)
const response = await fetch('https://bebeguide-chatbot.ttlthsusoky.workers.dev', {
```

   - 실제 Worker URL로 변경:

```javascript
// 변경 후 (실제 Worker URL)
const response = await fetch('https://bebeguide-chatbot.your-account.workers.dev', {
```

3. **저장 및 업로드**
   - 파일 저장
   - 웹 호스팅 서버에 업로드 (GitHub Pages, Cloudflare Pages 등)

---

## 🧪 테스트 방법

### 1. 로컬 테스트 (브라우저 개발자 도구)

```bash
# 간단한 로컬 서버 실행
cd C:\Users\hee\website
python -m http.server 8000
# 또는
npx serve
```

브라우저에서 `http://localhost:8000` 접속

### 2. 챗봇 테스트 시나리오

✅ **기본 기능 테스트**
- "0개월 체크리스트" → 체크리스트 안내
- "예방접종 일정" → 예방접종 정보
- "분유 얼마나 줘요?" → 분유 급여 팁

✅ **응급 상황 테스트**
- "아기가 고열이에요" → 즉시 119/병원 안내
- "호흡이 힘들어 보여요" → 응급 안내

✅ **참고사이트 정보 확인**
- "출처가 뭐예요?" → KDCA, WHO 등 언급
- AI 응답에 "KDCA 기준", "WHO 권장" 등 포함 확인

---

## 💰 비용 예상

### OpenAI API 요금 (GPT-4o-mini)

| 항목 | 비용 |
|------|------|
| 입력 1M 토큰 | $0.150 |
| 출력 1M 토큰 | $0.600 |
| **대화 1회 평균** | **약 $0.001 (1원)** |
| 대화 100회 | 약 $0.10 (100원) |
| 대화 1,000회/월 | 약 $1 (1,300원) |

### Cloudflare Workers 요금

| 항목 | 비용 |
|------|------|
| 무료 플랜 | 하루 10만 요청 무료 |
| 초과 시 | 100만 요청당 $0.50 |

**총 예상 비용**: 월 1,000명 사용 시 약 **$2-3 (2,600-3,900원)**

---

## 🔍 문제 해결

### 1. "API 키가 설정되지 않았습니다" 오류

**원인**: Cloudflare Worker 환경 변수 미설정

**해결**:
- Cloudflare Dashboard → Workers → bebeguide-chatbot
- Settings → Variables → Environment Variables
- `OPENAI_API_KEY` 변수 확인 및 재설정

### 2. "챗봇 응답 생성 중 오류" 메시지

**원인**: OpenAI API 키 잘못됨 또는 결제 정보 없음

**해결**:
- OpenAI Dashboard → API Keys 확인
- Billing → Payment method 등록 확인
- 키 재발급 후 Cloudflare에 재설정

### 3. 챗봇 버튼이 안 보임

**원인**: CSS 또는 JavaScript 파일 로딩 실패

**해결**:
- 브라우저 개발자 도구 (F12) → Console 확인
- `styles.css`, `script.js` 파일 경로 확인
- 캐시 삭제 후 새로고침 (Ctrl+Shift+R)

### 4. CORS 오류

**원인**: Worker URL이 잘못됨

**해결**:
- script.js의 fetch URL 확인
- Cloudflare Worker 배포 상태 확인
- Worker 설정에서 CORS 허용 확인 (이미 코드에 포함됨)

---

## 📊 모니터링

### Cloudflare Dashboard
- Workers → bebeguide-chatbot → Metrics
- 요청 수, 오류율, 응답 시간 확인

### OpenAI Dashboard
- https://platform.openai.com/usage
- API 사용량, 비용 확인

---

## 🔐 보안 권장사항

1. ✅ **API 키 암호화** - Cloudflare Encrypt 사용 (이미 설정됨)
2. ✅ **CORS 제한** - 필요시 특정 도메인만 허용
3. ✅ **Rate Limiting** - Worker에 요청 제한 추가 (선택)
4. ✅ **로그 모니터링** - 이상 패턴 감지

---

## 📝 참고사항

### 챗봇이 답변하는 내용

✅ **포함됨**:
- 질병관리청 (KDCA) - 예방접종 일정
- WHO - 모유수유, 신생아 건강
- 대한소아과학회 - 성장 발달
- 대한신생아학회 - 신생아 돌봄
- 미국소아과학회 (AAP) - SIDS 예방
- 식품의약품안전처 (MFDS) - 분유 안전
- 보건복지부 (MOHW) - 육아 정책
- 임신육아종합포털 아이사랑 - 월령별 가이드

❌ **답변 불가**:
- 구체적인 질병 진단
- 약물 처방 및 복용량
- 응급 의료 처치 (→ 119 안내)
- 범위 외 주제 (임신, 유아식, 교육)

---

## 🎉 완료!

챗봇이 정상 작동하면:

1. 웹사이트 방문자가 실시간 AI 상담 가능
2. 참고사이트 정보가 자동으로 답변에 포함됨
3. 응급 상황은 즉시 119/병원 안내
4. 이메일 문의는 기존 시스템 그대로 유지

---

## 📧 추가 지원

문제가 있으면:
1. OpenAI Discord: https://discord.gg/openai
2. Cloudflare Community: https://community.cloudflare.com
3. 이 문서 확인: C:\Users\hee\website\CHATBOT_SETUP.md
